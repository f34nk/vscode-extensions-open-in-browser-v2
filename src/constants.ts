/**
 * Extension-wide constants
 */

/**
 * The VS Code configuration namespace for this extension
 * Used with vscode.workspace.getConfiguration(APP_NAME)
 */
export const APP_NAME = 'open-in-browser';
